version: "3.9"

services:
  dnse-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dnse_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
